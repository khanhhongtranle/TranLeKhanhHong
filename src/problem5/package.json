{
  "name": "problem5",
  "version": "1.0.0",
  "description": "A crude server",
  "main": "src/index.ts",
  "scripts": {
    "start": "npx prisma generate && ts-node src/index.ts",
    "start:prod": "npx prisma migrate deploy && npx prisma generate && ts-node src/index.ts",
    "lint": "tslint -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@prisma/client": "^5.13.0",
    "@types/cors": "^2.8.17",
    "@types/dotenv": "^8.2.0",
    "@types/express": "^4.17.21",
    "prisma": "^5.13.0",
    "ts-node": "^10.9.2",
    "tslint": "^6.1.3",
    "typescript": "^5.4.5"
  }
}
